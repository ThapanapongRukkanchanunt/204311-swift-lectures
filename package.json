{
  "name": "204311-swift-lectures",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "dev": "npm run generate && vite --host 127.0.0.1",
    "build": "npm run generate && vite build && node scripts/package.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node scripts/check.mjs"
  },
  "dependencies": {
    "reveal.js": "6.0.2"
  },
  "devDependencies": {
    "fflate": "0.8.3",
    "vite": "8.3.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
