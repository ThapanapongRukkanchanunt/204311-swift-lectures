// W01-Lecture: constants, interpolation, and console output.
// Run with `swift Greeting.swift` where the Swift toolchain is installed.
let stop = "North Gate"
let minutes = 4
print("\(stop): \(minutes) min")
